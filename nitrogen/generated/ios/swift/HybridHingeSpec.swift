///
/// HybridHingeSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/// See ``HybridHingeSpec``
public protocol HybridHingeSpec_protocol: HybridObject {
  // Properties
  var widthClass: WidthSizeClass { get }
  var heightClass: HeightSizeClass { get }
  var windowWidth: Double { get }
  var windowHeight: Double { get }
  var safeInsets: SafeInsets { get }
  var fontScale: Double { get }
  var foldFeatures: [FoldFeature] { get }
  var hingeAngle: Double? { get }
  var deviceInfo: DeviceInfo { get }

  // Methods
  func addChangeListener(callback: @escaping (_ state: HingeState) -> Void) throws -> ChangeSubscription
  func getState() throws -> HingeState
}

public extension HybridHingeSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject Hinge]"
  }
}

/// See ``HybridHingeSpec``
open class HybridHingeSpec_base {
  private weak var cxxWrapper: HybridHingeSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridHingeSpec_cxx {
  #if DEBUG
    guard self is any HybridHingeSpec else {
      fatalError("`self` is not a `HybridHingeSpec`! Did you accidentally inherit from `HybridHingeSpec_base` instead of `HybridHingeSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridHingeSpec_cxx(self as! any HybridHingeSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the Hinge HybridObject.
 * Implement this protocol to create Swift-based instances of Hinge.
 * ```swift
 * class HybridHinge : HybridHingeSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridHingeSpec = HybridHingeSpec_protocol & HybridHingeSpec_base
