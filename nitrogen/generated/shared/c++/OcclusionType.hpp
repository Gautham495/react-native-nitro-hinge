///
/// OcclusionType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrohinge {

  /**
   * An enum which can be represented as a JavaScript union (OcclusionType).
   */
  enum class OcclusionType {
    NONE      SWIFT_NAME(none) = 0,
    FULL      SWIFT_NAME(full) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrohinge

namespace margelo::nitro {

  // C++ OcclusionType <> JS OcclusionType (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrohinge::OcclusionType> final {
    static inline margelo::nitro::nitrohinge::OcclusionType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return margelo::nitro::nitrohinge::OcclusionType::NONE;
        case hashString("full"): return margelo::nitro::nitrohinge::OcclusionType::FULL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum OcclusionType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrohinge::OcclusionType arg) {
      switch (arg) {
        case margelo::nitro::nitrohinge::OcclusionType::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case margelo::nitro::nitrohinge::OcclusionType::FULL: return JSIConverter<std::string>::toJSI(runtime, "full");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert OcclusionType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("full"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
